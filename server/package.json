{
  "name": "bikeriderapi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "ENVRIONMENT=local && npm run build && npm start",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && node esbuild.js",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Bell",
  "description": "Bike Connect API",
  "dependencies": {
    "@google-cloud/storage": "^7.16.0",
    "body-parser": "^2.2.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.16.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/pg": "^8.15.4",
    "esbuild": "^0.27.2",
    "typescript": "^5.8.3"
  }
}
